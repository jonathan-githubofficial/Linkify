import {render, screen, cleanup} from '@testing-library/react'

// import ProfileHeadline from '../components/profile/HeadlineTop';

test('should render profile headline component', () => {
  expect(true).toEqual(true);
});